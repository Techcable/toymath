[package]
name = "extended-float-sys"
version = "0.1.0"
authors = ["Techcable <Techcable@techcable.net>"]
# It's a toy and probably isn't very correct
publish = false
build = "build.rs"

[dependencies]
libc = "0.2.42"

[build-dependencies]
cc = "1.0"
